# دليل إعداد Supabase

هذا الدليل يشرح كيفية ربط المنصة بقاعدة بيانات Supabase للحصول على تخزين دائم وموثوق.

## 📋 المتطلبات

1. حساب على [supabase.com](https://supabase.com) (الباقة المجانية كافية).
2. مشروع منشور على Netlify.

## 🚀 الخطوات

### 1. إنشاء مشروع Supabase

1. اذهب إلى [supabase.com](https://supabase.com) وسجّل دخولك.
2. اضغط **New Project**.
3. اختر اسماً للمشروع (مثل `edu-platform`).
4. اختر كلمة مرور قوية لقاعدة البيانات واحفظها.
5. اختر المنطقة الأقرب لمستخدميك (مثل `Frankfurt` لأوروبا أو `Singapore` لآسيا).
6. اضغط **Create new project** وانتظر دقيقة حتى يكتمل الإعداد.

### 2. إنشاء الجداول

1. من القائمة الجانبية، اذهب إلى **SQL Editor**.
2. اضغط **New query**.
3. افتح ملف `supabase_schema.sql` من هذا المشروع.
4. انسخ المحتوى بالكامل والصقه في محرر SQL.
5. اضغط **Run** (أو Ctrl+Enter).
6. ستظهر رسالة `تم إنشاء المخطط بنجاح!` وقائمة بالجداول.

### 3. الحصول على مفاتيح API

1. من القائمة الجانبية، اذهب إلى **Project Settings** (⚙️).
2. اضغط **API**.
3. ستجد ثلاثة معلومات مهمة:
   - **Project URL**: مثل `https://xyzcompany.supabase.co`
   - **anon public key**: مفتاح عام (لا يستخدم للكتابة).
   - **service_role key**: مفتاح سرّي للخادم فقط ⚠️.

### 4. إعداد متغيرات البيئة في Netlify

1. اذهب إلى لوحة تحكم Netlify لمشروعك.
2. **Site settings** → **Environment variables** → **Add a variable**.
3. أضف المتغيرات التالية:

| المتغير | القيمة | الوصف |
|--------|-------|-------|
| `SUPABASE_URL` | `https://xyzcompany.supabase.co` | رابط مشروع Supabase |
| `SUPABASE_SERVICE_KEY` | `eyJhbGci...` | مفتاح service_role (سرّي!) |
| `SUPABASE_ANON_KEY` | `eyJhbGci...` | مفتاح anon (اختياري) |
| `NETLIFY_AUTH_SECRET` | أي نص عشوائي طويل | سرّ توقيع التوكنات |
| `ADMIN_EMAIL` | `admin@platform.site` | بريد الإدارة الافتراضي |
| `ADMIN_PASSWORD` | `admin123` | كلمة مرور الإدارة (غيّرها!) |

4. اضغط **Save**.
5. أعد نشر الموقع (**Deploys** → **Trigger deploy** → **Deploy site**).

### 5. التحقق من العمل

1. بعد النشر، اذهب إلى `/admin/login.html`.
2. سجّل دخول بـ `admin@platform.site` / `admin123`.
3. من **الإعدادات** → **حالة النظام**، يجب أن ترى:
   - وضع التخزين: `supabase`
   - Supabase مُعد: `نعم`

## 🔒 الأمان

### Row Level Security (RLS)
- كل الجداول تستخدم RLS.
- السياسة الافتراضية: `USING (false) WITH CHECK (false)` — تمنع كل الوصول العام.
- مفتاح `service_role` يتخطى RLS تلقائياً — لذلك يعمل فقط من الخادم.

### مفتاح service_role
- ⚠️ **لا تضع مفتاح service_role في كود الواجهة الأمامية أبداً!**
- يُستخدم فقط في Netlify Functions (يبقى على الخادم).
- لو تسرّب، يمكنك تجديده من إعدادات Supabase.

### مفتاح anon
- آمن للواجهة الأمامية (لكن لا نستخدمه هنا).
- لا يسمح بالكتابة بسبب RLS.

## 🔄 الترحيل من JSON/Blobs إلى Supabase

الموقع يدعم 3 أوضاع تخزين تلقائياً:
1. **Supabase** (الإنتاج) — إذا ضُبطت `SUPABASE_URL` و `SUPABASE_SERVICE_KEY`.
2. **Netlify Blobs** (الإنتاج) — fallback تلقائي.
3. **JSON محلي** (المعاينة) — للتحميل المحلي.

الترتيب تلقائي: يجرب Supabase أولاً، ثم Blobs، ثم JSON.

### ترحيل البيانات الموجودة

لو كان لديك بيانات في Blobs وتريد نقلها لـ Supabase:
1. من لوحة الإدارة → **البيانات** → **تصدير JSON**.
2. اضبط متغيرات Supabase في Netlify.
3. أعد النشر.
4. من لوحة الإدارة → **البيانات** → **استيراد JSON** (الصق البيانات المصدّرة).

## 🛠️ استكشاف الأخطاء

### خطأ: "SUPABASE_URL أو SUPABASE_SERVICE_KEY غير مضبوطين"
- تحقق من متغيرات البيئة في Netlify.
- تأكد من نشر الموقع بعد إضافة المتغيرات.

### خطأ: "تعذّر الاتصال بـ Supabase"
- تحقق من صحة `SUPABASE_URL` (يجب أن يبدأ بـ `https://` وينتهي بـ `.supabase.co`).
- تحقق من عدم توقف مشروع Supabase (الباقة المجانية تتوقف بعد أسبوع من عدم النشاط).

### خطأ: "مفتاح Supabase السرّي غير صالح"
- تأكد من نسخ مفتاح `service_role` وليس `anon`.
- تأكد من عدم وجود مسافات في بداية/نهاية المفتاح.

### البيانات لا تظهر بعد الإعداد
- اذهب إلى Supabase → **Table Editor** وتحقق من وجود بيانات في الجداول.
- من لوحة الإدارة → **البيانات** → **تصدير JSON** للتأكد.

## 📊 مراقبة الاستخدام

من لوحة Supabase:
- **Database** → **Reports** — حجم قاعدة البيانات والاستعلامات.
- **Authentication** → **Users** — (لا نستخدم مصادقة Supabase، فقط الجداول).
- **Logs** → سجلات الاستعلامات.

الباقة المجانية تشمل:
- 500MB تخزين.
- 50,000 صف شهرياً (أكثر من كافٍ للمنصات الصغيرة).

## ❓ أسئلة شائعة

**س: هل أحتاج لتثبيت أي شيء؟**
ج: لا. المكتبة تستخدم `fetch` الأصلي في Node 18+.

**س: هل يعمل الموقع بدون Supabase؟**
ج: نعم، يستخدم Netlify Blobs أو JSON محلي تلقائياً.

**س: هل يمكنني استخدام قاعدة بيانات أخرى؟**
ج: نعم، لكن ستحتاج لتعديل `netlify/functions/supabase.mjs`.

**س: ماذا يحدث لو توقف Supabase؟**
ج: الموقع سيرفض الطلبات (لن يعمل fallback تلقائياً بعد بدء استخدام Supabase).
   لو أردت العودة لـ Blobs، احذف متغيرات `SUPABASE_*` من Netlify.
